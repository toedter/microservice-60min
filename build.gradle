apply from: "$rootDir/gradle/versioning.gradle"

allprojects {
    apply plugin: 'idea'
    apply plugin: 'eclipse'
    group = 'com.toedter'
}

task wrapper(type: Wrapper) {
    gradleVersion = '4.8.1'
}

